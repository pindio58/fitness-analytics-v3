apiVersion: v1
kind: Service
metadata:
  name: fitness-analytics-metabase-service
  namespace: fitness-analytics-namespace
spec:
  selector:
    app: metabase
  ports:
    - port: 3000
      targetPort: 3000
